# Next Phase Research Plan From Phase 10

Date: `2026-04-23`

## Current Locked State

The evidence stack is now locked through `Phase 10`.

Current ladder:

1. `V7` - behavioral lattice/control separation
2. `V8` - late-layer internal hidden-state separation
3. `Phase 2` - rerun / variance lock
4. `Phase 3` - dimension and late-band structure
5. `Phase 4` - token-path localization and localization variance
6. `Phase 5` - context-to-readout bridge map
7. `Phase 6` - `PennyLane` quantum-bridge encoding discovery
8. `Phase 7` - `Qiskit` mirror / simulator lock
9. `Phase 8` - Bell-state calibration
10. `Phase 9` - real IBM Quantum hardware bridge
11. `Phase 9B` - IBM hardware repeatability
12. `Phase 9C` - direct PennyLane remote hardware pass
13. `Phase 9D` - direct PennyLane hardware repeatability
14. `Phase 10` - Bell-type semantic contextuality lock on compressed semantic feature states

## Phase 10 Read

`Phase 10` formalizes the semantic contextuality rung on the compressed
AI-feature state layer.

Locked read:

- semantic label buckets `A`, `A'`, `B`, and `B'` are now fixed
- family-local measurement vectors are resolved from the compressed state geometry
- all `8/8` compressed semantic feature states show `S_max > 2`
- matched unentangled controls remain exactly at `2.0`
- this is a semantic / AI-side contextuality result, not a physical Bell nonlocality claim

That means the stack now has:

- behavioral separation
- internal hidden-state separation
- localization and bridge structure
- real IBM hardware bridge execution
- direct PennyLane hardware repeatability
- a formal Bell-type semantic contextuality protocol rung

## Next Rung - Phase 11

### Objective

Carry the locked `Phase 10` semantic settings onto real IBM hardware through
both the `Qiskit` and direct `PennyLane` paths.

### Success Condition

- semantic setting direction survives real hardware noise
- same-backend and cross-backend repeat preserve the sign structure
- framework-path differences are documented without changing the underlying read
- public docs keep the boundary clear: semantic contextuality on hardware, not yet a physical Bell nonlocality claim

## After Phase 11

The next bridge after hardware semantic repeatability is the recovered physical
control lane:

1. `Phase 12A` - `ARC15` isolated physical-control sessions
2. `Phase 12B` - bounded `EEG + HRV` capture sessions
3. `Phase 12C` - synchronized `ARC15 + EEG/HRV + backend timing` overlays

That preserves the hierarchy:

- semantic / AI-side contextuality first
- hardware semantic repeatability second
- physical control and biological measurement after that

## Nesting Continuation

After the semantic and physical-control ladders are stable, the broader nesting
path remains:

1. smallest formal systems
2. constrained structured systems
3. classical coherence systems
4. biological comparator classes
5. multi-class convergence test

The posture stays the same:

- under-promise
- over-deliver
- let each rung make the next claim more legible
