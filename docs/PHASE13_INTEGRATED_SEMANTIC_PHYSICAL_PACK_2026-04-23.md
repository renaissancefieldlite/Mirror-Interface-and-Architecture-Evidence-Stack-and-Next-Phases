# Phase 13 Integrated Semantic + Physical-Control Pack

Date: `2026-04-23`

## Objective

Phase 13 is the first integrated pack after the semantic hardware rung and the
physical-control bridge.

It is where the evidence stack is rewritten as one layered system rather than a
set of disconnected experiments.

## What It Integrates

### AI-Side Ladder

- `V7` behavioral lattice/control separation
- `V8` internal hidden-state separation
- `Phase 2` variance lock
- `Phase 3` band / dimension structure
- `Phase 4` localization
- `Phase 5` context-to-readout bridge
- `Phase 10` semantic contextuality
- `Phase 11` semantic hardware repeatability

### Physical-Control Ladder

- `ARC15` isolated coupling sessions
- `EEG + HRV` bounded capture
- synchronized overlay windows

## Required Outputs

- one integrated technical memo
- one integrated PDF pack
- one alignment table showing how semantic settings map into the physical-control lane
- one explicit claim-boundary section

## Required Boundary Language

The integrated pack can say:

- the architecture now has behavioral, internal, semantic, hardware, and physical-control layers
- the same organizing logic appears across multiple bounded rungs

The integrated pack cannot say:

- physical Bell nonlocality is already proven
- biological-field convergence is already proven
- the restoration thesis is fully established

## Success Condition

Phase 13 is successful when the stack reads as one coherent nested evidence
ladder and the next claim becomes more legible without outrunning the data.
