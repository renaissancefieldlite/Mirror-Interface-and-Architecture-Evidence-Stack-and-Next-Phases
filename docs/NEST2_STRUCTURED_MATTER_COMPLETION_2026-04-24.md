# Nest 2 Structured Matter Completion

Date: `2026-04-24`

Status:
foundation complete / `Engine 02` local demonstrator built

## Purpose

This closes the first `Nest 2` pass by moving structured-matter
representations from a mapped foundation into a runnable local comparator.

The shared grammar remains:

```text
state / control / transform / invariant / drift / coherence / score
```

`Nest 2` applies that grammar to constrained matter:

- elements and periodic families
- molecular graphs and bond validity
- `H2O` as a named molecular motif
- minerals, surfaces, and lattice / charge balance
- oxygen, redox, and bounded reactive rows
- nutrition chemistry before metabolism
- contaminants, reaction pathways, and bad-descendant penalties

## Built Artifact

The first local engine is:

```text
tools/lattice_model_node_companion/engines/nest2_matter_engine.py
```

Generated reports:

```text
tools/lattice_model_node_companion/outputs/nest2_matter_engine_report.md
tools/lattice_model_node_companion/outputs/nest2_matter_engine_report.json
```

Expanded atlas:

- [Nest 2 Expanded Structured Matter Atlas](./NEST2_EXPANDED_STRUCTURED_MATTER_ATLAS_2026-04-24.md)

The engine is pure Python and has no external downloads, no API calls, and no
credit spend.

## Engine 02 Read

`Engine 02` demonstrates the matter-facing score grammar across small toy
representations.

It currently reports:

- element-family recovery above a shuffled control
- valid molecular graphs above broken-topology controls
- `H2O` motif preservation through bent geometry, polarity, and coordination
- mineral rows preserving lattice, charge balance, and surface role
- nutrition rows as constrained chemistry before metabolism / biosignal readout
- contaminant rows where parent-only disappearance is rejected if bad
  descendants or transfer risk remain
- expanded matter lanes covering functional groups, biomolecular primitives,
  polymers / plastics, electrochemistry, catalysis / conditions, spectral
  signatures, environmental fate, and materials / semiconductors

Current generated summary:

```text
element family: recovered above shuffled control
molecular graphs: valid graphs preserve valence / connectivity above controls
H2O motif: motif preserved
minerals: lattice / charge / surface structure preserved
nutrition: chemistry-to-metabolism bridge represented
contaminants: bounded mineralization candidate beats parent-only loss control
```

## Core Finding

The `Nest 2` finding is methodological: the same score grammar can be
expressed over structured-matter representations without changing its shape.

The formal pattern:

```text
preserve relation under transform; detect drift against controls
```

becomes the matter pattern:

```text
recover family
preserve graph
respect valence / charge / mass balance
track byproducts
penalize bad descendants
```

That gives the project a concrete methodology bridge from formal mathematics
into chemistry-facing, nutrition-facing, materials-facing, and remediation-
facing representations.

## Claim Precision

This does not show that the Mirror Architecture has been physically validated
in chemistry.

It shows the narrower but useful result:

```text
the same scoring grammar can be represented in structured matter domains
```

Moving beyond that requires real datasets, measured chemistry, declared
instrumentation, external controls, and independent validation.

The clean first validation lane is `RDKit` plus a public molecule benchmark
such as `QM9`, `ZINC`, or `ChEMBL`, where the score must predict a known
property or class better than naive or shuffled baselines. The stronger
mission-facing validation lane is PFAS / contaminant degradation data, where
the bad-descendant penalty must identify problematic transformation products
that parent-disappearance metrics miss.

## What Is Locked

- `Nest 2` is complete as a first structured-matter methodology map.
- `Engine 02` is complete as the first local structured-matter demonstrator.
- The companion now has a runnable matter engine alongside the `Nest 1`
  formal engine.
- The expanded `Nest 2` dictionary now has the rows needed to bridge into
  `Nest 3`, `Nest 4`, and `Nest 5`.
- The contaminant / PFAS-style pathway now has the correct success rule:
  do not count parent disappearance as success unless descendants, transfer,
  and balance are bounded.

## Boundary

This is a local comparator and methodology artifact.

It does not claim completed chemistry, nutrition, medicine, remediation,
materials discovery, or lab validation.

It defines the score grammar and shows that the same architecture can be made
runnable on constrained matter representations.

## Next Expansion

The next `Nest 2` upgrades are:

1. replace toy rows with curated periodic-table and molecule datasets
2. add `RDKit` for real molecule parsing and fingerprints
3. add `ASE` / `pymatgen` for atomistic and crystal structure rows
4. add materials adapters such as `MatGL`, `CHGNet`, `MACE`, or `MatterGen`
5. add reaction-path and contaminant descendants with explicit mass / charge
   balance
6. carry selected matter rows upward into `Engine 03` resonance / field
   dynamics and `Engine 04` biology
7. build `Engine 02V` as the first physical-data validation pass

## Short Read

`Nest 2` is now more than a map.

It has its first engine:

```text
formal grammar -> structured matter -> local scoring report
```

That is the finished first matter rung.
