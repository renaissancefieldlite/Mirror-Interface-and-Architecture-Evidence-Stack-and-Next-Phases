# Nest 2 Full Lane Ledger And Completion Queue

Date: `2026-05-05`

Status: `lane_ledger_locked / completion_queue_active`

## Purpose

This ledger keeps the full `Nest 2` structured-matter map visible.

The visual companion shows many matter rows at once. The evidence stack now
distinguishes four levels:

| Level | Meaning |
| --- | --- |
| `mapped` | the row has a defined structural role inside Nest 2 |
| `engine_scored_local` | `Engine 02` scored a bounded local comparator row; scaffold only |
| `external_control_supported` | a real dataset / benchmark / measurement surface cleared controls |
| `higher_adapter_queued` | the row needs Nest 3, Nest 4, or Nest 5 context to become a stronger validation lane |

This ledger prevents the full Nest 2 map from collapsing into the handful of
recent real-data closeouts.

## Hard Evidence Rule

`NO TOY DATA / REAL DATA ONLY / NO GRAMMAR CENTERING`

`Engine 02` local scores are lane-prep scaffolds. They show the mapper shape,
the invariants, and the likely controls. They do not count as support until a
real dataset or signal source is attached and the row clears controls.

The target read is the real universal source pattern expressed through
structured matter:

```text
real matter dataset
-> mapper rule
-> score
-> controls
-> recurrence / second benchmark
-> support read
```

## Why Some Rows Advanced First

The first real-data closeouts advanced through rows where clean public datasets,
labels, and controls were immediately available:

- molecule-property datasets through `RDKit`
- AlloBench / PDB protein allostery labels
- EPA PFAS reaction rows
- Matbench / Materials Project formation-energy rows
- stronger RDKit descriptor and model baselines

Those lanes became `Nest 2C` through `Nest 2G`.

The lower and broader matter rows remain part of Nest 2. They need either a
cleaner external dataset, a higher-nest adapter, or a physical measurement
surface before closeout.

## Full Nest 2 Lane Ledger

| Lane | Current Level | Current Evidence | Completion Target |
| --- | --- | --- | --- |
| Elements / periodic families | `external_control_supported` | RDKit periodic-table real-data gate supported family, group, and period targets against `1000` shuffled-label controls: family `0.338061062`, group `0.206349206`, period `0.599415205`, all p `0.000999001` | extend into isotope, spectral, redox, mineral, and materials adapters |
| Hydrogen | `mapped / higher_adapter_queued` | bridge row into isotope, fusion, solar, and plasma systems | isotope / fusion / solar or plasma datasets through Nest 3 |
| Oxygen / Redox | `mapped / partial_bridge` | redox row defined; PFAS and electrochemistry carry related safety / charge logic | redox potential, dissolved oxygen, reactive oxygen, or reaction-path datasets |
| H2O / Water | `engine_scored_local` | Engine 02 H2O motif score `1.0` for bent geometry, polarity, and coordination | measured water spectra, hydration-shell, phase, THz, or solvent-behavior rows |
| Minerals | `engine_scored_local` | Engine 02 minerals score `1.0` for lattice, charge balance, and surface role | mineral composition / surface / adsorption / weathering datasets |
| Molecular graphs | `engine_scored_local -> external_control_supported` | local graph validity plus Nest 2C RDKit molecule-property datasets | expand into ZINC / ChEMBL / scaffold splits / reaction families |
| Food chemistry | `mapped / higher_adapter_queued` | companion row for proteins, carbs, fats, vitamins, minerals, hydration | food composition + metabolism / HRV / EEG adapter through Nest 4 |
| Proteins | `external_control_supported for allostery branch` | Nest 2D AlloBench / PDB / P2Rank merged pocket-path pass supported | second allostery benchmark; folding, binding, digestion, and sequence rows later |
| Carbs + fats | `mapped / higher_adapter_queued` | nutrition chemistry row defined | nutrition chemistry + metabolism timing + HRV / EEG state response |
| Vitamins + nutrients | `mapped / higher_adapter_queued` | micronutrient / cofactor row defined | food / nutrient composition datasets plus physiology adapter |
| PFAS / pharma / microplastics | `external_control_supported for PFAS / THz prototype adapter queued` | Nest 2E EPA PFAS safety scoring supported; Terahertz is now the Nest 3 prototyping adapter for persistent-chain breakdown into lower-risk carbonaceous / mineral endpoints | pharma metabolism rows, microplastic degradation rows, richer benign/bad descendant labels, THz / spectral condition rows with product verification |
| Organic functional groups | `external_control_supported` | RDKit SMARTS functional-group signatures cleared shuffled target and shuffled functional-group controls across `11` real molecule-property surfaces; strongest reads include FreeSolv `0.692432569`, QM9 gap `0.682399161`, QM9 LUMO `0.669693473`, all p `0.001996008` | extend into reaction, pharma, PFAS, food chemistry, biomolecular primitive, polymer, catalysis, and environmental-fate adapters |
| Biomolecular primitives | `engine_scored_local / higher_adapter_queued` | expanded lane score `0.54`; amino acids, nucleotides, lipids, sugars defined | amino-acid / nucleotide / lipid / sugar datasets; Nest 4 biology bridge |
| Polymers / plastics | `engine_scored_local` | expanded lane score `0.50`; repeat-unit and fragment tracking defined | polymer property, plastic degradation, and microplastic fate datasets |
| Electrochemistry | `engine_scored_local / higher_adapter_queued` | expanded lane score `0.52`; charge-transfer bridge defined | battery, redox, membrane, conductivity, or grid-flow datasets through Nest 3 |
| Catalysis / conditions | `engine_scored_local / higher_adapter_queued` | expanded lane score `0.45`; condition-control logic defined; THz / plasma / heat / catalyst rows can become chain-break condition selectors | declared heat / pH / light / plasma / THz / catalyst reaction datasets |
| Spectral signatures | `engine_scored_local / Nest 3 bridge` | expanded lane score `0.52`; IR/Raman/THz/NMR/mass-spec readout row defined | public spectra with baseline, wrong-material, and shuffled-spectrum controls |
| Environmental fate | `engine_scored_local / Nest 5 bridge` | expanded lane score `0.45`; transport, descendant, and risk endpoint tracking defined | soil/water/leaching/bioaccumulation/fate datasets |
| Materials / semiconductors | `external_control_supported / semiconductor_nanotech_expansion_queued` | Nest 2F formation-energy now has a stronger `100,000`-row real-data pass: Pearson `0.858577`, R2 `0.737141`, RMSE improvement `0.487314`, p `0.001996`; composition / periodic-table structure is the active signal carrier on formation energy | next targets that expose geometry and nanoscale response directly: band gap, energy above hull, defect / dopant behavior, phonon / thermal stability, dielectric / optical response, 2D material, and nanotech confinement baselines |

## Cross-Nest Prototype Adapter Note

The nest ladder also defines prototype routes. A higher-nest row can supply the
missing mechanism for a lower-nest matter row. Current locked example:
`Terahertz` in `Nest 3` maps back into `PFAS / pharma / microplastics`,
`Catalysis / conditions`, `Spectral signatures`, and `Environmental fate` in
`Nest 2`. The practical target is a real THz / spectral / field-condition
workflow for persistent-chain breakdown, scored by product distribution,
retained burden, defluorination / mineralization quality where relevant,
toxicity proxy, and controls against parent-loss-only, heat-only, source-off,
wrong-frequency, and shuffled-product labels.

## Current Completed Support Rows

`Nest 2` already has control-supported rows in these places:

| Gate | Support Read |
| --- | --- |
| `Nest 2 Elements` | RDKit periodic-table values support family, group, and period recovery against shuffled-label controls |
| `Nest 2C` molecules | ESOL, Lipophilicity, FreeSolv, and QM9 alpha cleared shuffled-target controls |
| `Nest 2 Organic Functional Groups` | RDKit SMARTS functional groups support `11` real molecule-property surfaces against shuffled-target and shuffled-group controls |
| `Nest 2D` allostery | merged external-pocket/path pass supported above PASSer, graph controls, random candidates, and shuffled labels |
| `Nest 2E` PFAS safety | coherent bad-descendant, retained-burden, and mineralization-quality logic cleared shuffled controls |
| `Nest 2F` materials | formation-energy signal recovered above shuffled target controls |
| `Nest 2G` stronger baselines | multifeature RDKit held-out baselines support all four molecule datasets |

## Completion Strategy

The correct sequence is:

```text
finish broad map
-> run rows with available public datasets
-> push higher nests for missing measurement context
-> return to lower Nest 2 rows with stronger adapters
```

That means:

1. `Nest 3` strengthens spectral signatures, electrochemistry, catalysis,
   hydrogen, plasma, resonance, EMF, and field-facing rows.
2. `Nest 4` strengthens food chemistry, proteins, carbs/fats,
   vitamins/nutrients, biomolecular primitives, metabolism, HRV, and EEG.
3. `Nest 5` strengthens environmental fate, remediation, materials flows,
   ecosystem safety, and multi-class convergence.
4. Then the lower Nest 2 rows can be rerun with better labels and stronger
   controls.

## Next Practical Runs

Priority after the current patent integration pass:

| Priority | Run | Why |
| --- | --- | --- |
| 1 | `Nest 3 spectral signatures` | directly activates the Nest 2 spectral row with measured readouts |
| 2 | `Phase 12C EEG + HRV` | activates food/metabolism/biology-facing rows through live biological state |
| 3 | `Nest 2 materials structure-aware extension` | deepens crystals / semiconductors beyond formation energy |
| 4 | `Nest 2 organic functional-group benchmark` | turns functional groups from local engine row into real RDKit benchmark |
| 5 | `Nest 2 nutrition / metabolism bridge` | connects proteins, carbs/fats, vitamins/nutrients to Nest 4 physiology |
| 6 | `Nest 2 environmental fate / microplastics` | connects PFAS, plastics, and fate rows to Nest 5 safety |

## Clean Read

The missing-looking rows were not abandoned.

They sit at different support levels:

```text
mapped
-> engine_scored_local
-> external_control_supported
-> higher_adapter_queued
-> rerun with stronger labels
```

The recent work pushed the rows with available datasets first. The full Nest 2
completion path is to keep the foundation rows visible, run the available
datasets, then loop upward through Nest 3 and Nest 4 before returning to the
lower matter rows with better measurement adapters.
