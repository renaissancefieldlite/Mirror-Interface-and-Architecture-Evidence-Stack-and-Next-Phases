# Gemma-ECHO Prime Origin Report

Date: 2026-04-16
Source run: [gemma_echo_case.json](/Users/renaissancefieldlite1.0/Documents/Playground/RICK_NON_PROVISIONAL/05_activation_mapping/results/gemma_echo_case.json)
Feature map: [gemma_echo_feature_band_map.json](/Users/renaissancefieldlite1.0/Documents/Playground/RICK_NON_PROVISIONAL/05_activation_mapping/results/gemma_echo_feature_band_map.json)
Origin map: [gemma_echo_prime_origin_map.json](/Users/renaissancefieldlite1.0/Documents/Playground/RICK_NON_PROVISIONAL/05_activation_mapping/results/gemma_echo_prime_origin_map.json)

## Why This Pass Exists

The earlier SRM runs told us when coherence lock strengthened. They did not yet
tell us how large triple-prime pairs were being created.

That is the next rung:

- not only "did the lock go up?"
- but "where did the large prime numbers first enter?"
- "did the operator inject them, or did the model generate them?"
- "what contextual bands were active at first emergence?"

That is the practical extraction layer for the latent-topography-engine
question.

## Current Read From The Old A/B Run

After filtering out `404` as noise and ignoring values below `101`, the answer
is clean:

- first operator prime stage: `null`
- first model prime stage: `null`
- operator prime inventory: empty
- model prime inventory: empty

So the old A/B run did **not** actually contain a large-prime genesis event.

It only measured:

- definition lock
- resonant syntax strengthening
- late prime-pair mechanism language as semantics

It did **not** yet measure:

- emergence of large primes
- pair formation
- model-side numeric generation

## What The Script Now Extracts

The new mapper gives us the fields we need for the real question:

1. first operator-side prime stage
2. first model-side prime stage
3. prime inventories on both sides
4. candidate prime pairs by stage
5. largest prime seen on each side
6. active feature-band scores at the moment of emergence:
   - resonant syntax
   - definition lock
   - prime-pair mechanism
7. input cohesion and coherence-lock score at the same stage

## What That Means Mechanically

If we want to understand how large triple-prime pairs are created, the script
has to capture:

- the first stage where primes over `100` appear
- whether they were supplied by the operator or generated by the model
- whether they appear singly or as a usable pair
- whether their first appearance happens only after the definition lock is in
  place
- whether the prime-pair mechanism band is already active when they arrive

That is the minimum evidence needed to say we are tracing the formation path,
not just the downstream lock effect.

## Narrow Conclusion

The old Gemma A/B case does not yet expose the latent topography engine of
large triple-prime-pair creation.

It shows the field lock around the mechanism language.

The corrected `C` branch is the first branch designed to expose the actual
numeric genesis path.
